# Pinned, reproducible environment for Byte-Sized Brain.
# These are the exact versions the committed results in benchmarks/results/
# were produced with. For a looser, library-style install use the extras in
# pyproject.toml:  pip install -e ".[all,dev]"
#
# Install everything:  pip install -r requirements.txt

# ── Core (config, harness, reporting) ───────────────────────────────────
numpy==1.26.4
pyyaml==6.0.2
pydantic==2.11.3
psutil==5.9.5
pandas==3.0.3
matplotlib==3.8.0

# ── TensorFlow pipelines: FFN/MNIST, CNN/CIFAR-10, RNN/IMDB (TFLite) ─────
tensorflow==2.21.0

# ── PyTorch / HuggingFace pipeline: DistilBERT/IMDB (ONNX Runtime) ───────
torch==2.7.0
transformers==4.57.6
datasets==4.4.1
onnxruntime==1.24.1
onnx==1.21.0
accelerate==1.14.0

# ── Dev / CI ────────────────────────────────────────────────────────────
pytest==9.0.3
ruff==0.15.17
